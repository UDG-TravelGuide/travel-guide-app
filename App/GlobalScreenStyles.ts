import { StyleSheet } from 'react-native';

//App Styles
const styles = StyleSheet.create({ general: {} });
export default styles;

//Colors
export const colors = { primaryOrange: '#FF690F', backgroundColor: '#FFF6F0' };
